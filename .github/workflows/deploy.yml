name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy-job:
    runs-on:
      - self-hosted
      - Linux
      - X64

    steps:
      - name: Repo'yu checkout et
        uses: actions/checkout@v3

      - name: Çalışma dizinini kontrol et
        run: |
          pwd
          ls -la
          ls -la yemek-tarifi/yemek-tarifi

      - name: Deploy işlemi
        run: |
          echo "Buraya deploy komutlarını ekle"
          # Örneğin, scp ile dosya kopyalama:
          # scp -r yemek-tarifi/yemek-tarifi/* user@sunucu:/var/www/html/

